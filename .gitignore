# Screenshots are generated — only keep the .gitkeep unless you want to commit them
# screenshots/*.png

# Host-specific config (copy config.example.json → config.json to use)
config.json

# Python
__pycache__/
*.pyc
.venv/
